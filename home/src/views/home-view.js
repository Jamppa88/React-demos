import React from 'react';

export default function Home(props) {

    

    return(
        <div>
        <h2>This is home!</h2><button onClick={props.e}>click to change</button>
        </div>
    );
}
